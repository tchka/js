/**
 * Объект с настройками игры
 * @property {int} rowsCount количество строк
 * @property {int} colsCount количество столбцов
 */
'use strict';
const config = {
    rowsCount: 8,
    colsCount: 8,
};